---
name: ts-practices
description: TypeScript coding practices and conventions to follow when writing or changing TypeScript or JavaScript code. Use whenever creating or editing .ts, .tsx, .js, or .jsx files, or doing TypeScript work in general.
---

# TS Practices

Practices to follow when doing TypeScript work in this project.

## Promises

Prefer `await` over `.then()` chains, and `try`/`catch` over `.catch()`.

## Types

Prefer annotating return types on functions and methods.
